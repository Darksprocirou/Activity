<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Actividad"
        tools:targetApi="31">
        <activity
            android:name=".MostrarUsuariosRegistrados"
            android:exported="false" />
        <activity
            android:name=".MostrarUsuarios"
            android:exported="false"
            android:label="@string/title_activity_mostrar_usuarios"
            android:theme="@style/Theme.Actividad" />
        <activity
            android:name=".MostrarTablaUsuarios"
            android:exported="false"
            android:label="@string/title_activity_mostrar_tabla_usuarios"
            android:theme="@style/Theme.Actividad" />
        <activity
            android:name=".MainActivity2"
            android:exported="false"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.Actividad" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>